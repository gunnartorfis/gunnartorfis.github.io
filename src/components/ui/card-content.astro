---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className, ...props } = Astro.props;
---

<div class={cn("font-mono text-sm text-pretty", className)} {...props}>
  <slot />
</div>
