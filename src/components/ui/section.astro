---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className, ...props } = Astro.props;
---

<section
  class={cn(
    "flex min-h-0 flex-col gap-y-4 print:gap-y-1 leading-6 antialiased font-sans ",
    className
  )}
  {...props}
>
  <slot />
</section>
