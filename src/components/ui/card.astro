---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className, ...props } = Astro.props;
---

<div
  class={cn("bg-card text-card-foreground rounded-lg", className)}
  {...props}
>
  <slot />
</div>
