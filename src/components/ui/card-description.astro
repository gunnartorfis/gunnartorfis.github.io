---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className, ...props } = Astro.props;
---

<p class={cn("text-foreground text-sm", className)} {...props}>
  <slot />
</p>
